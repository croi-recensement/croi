<form method="post" action="{{ path('santes_delete', {'id': sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sante.id) }}">
    <button class="btn">Delete</button>
</form>
