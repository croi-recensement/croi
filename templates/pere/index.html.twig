{% extends 'base.html.twig' %}

{% block title %}Sante{% endblock %}

{% block content %}
    <div id="page-inner">   
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="row">
                    <div class="col-md-4">PERE</div>
                    <div class="col-md-4"></div>
                    <div class="col-md-4">
                </div>
            </div>
        </div>
        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover" style="font-size: 10px">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Groupe Sanguin</th>
                        <th>Etat</th>
                        <th>Maladie Chronique</th>
                        <th>Maladie Cardiaque</th>
                    </tr>
                </thead>
                <tbody>
                {% for pere in peres %}
                    <tr>
                        <td>{{ pere.id }}</td>
                        <td>
                            <a href="{{ path('app_dashboard_dad_read', {'id': pere.id}) }}" class="btn btn-info"><i class="fa fa-eye"></i></a>
                            <a href="{{ path('app_dashboard_dad_edit', {'id': pere.id}) }}" class="btn btn-warning">i<i class="fa fa-edit"></i></a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="22">Pas de donnée</td>
                    </tr>
                {% endfor %}
                </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
